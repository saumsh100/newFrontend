
import React, { PropTypes } from 'react';
import RInfiniteScroller from 'react-infinite-scroller';

export default function InfiniteScroll(props) {
  return <RInfiniteScroller {...props} />;
}

InfiniteScroll.propTypes = {};
