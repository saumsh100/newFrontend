
import PropTypes from 'prop-types';

const chairShape = {
  id: PropTypes.string,
  accountId: PropTypes.string,
  description: PropTypes.string,
  isActive: PropTypes.bool,
  pmsId: PropTypes.string,
  name: PropTypes.string,
};

export default chairShape;
